[x,fs,nbits]=wavread('rexdata/rex9.wav');
sound(x,fs);

L = spect(x);

% axis([1,size(L,2),1,128]);

pcolor(L);
% axis([1500 2000 1 128]);
axis([1,size(L,2),7,11]);
shading('flat');
% surf(L); shading('flat');
colormap(hsv);

avg = 56;
len = 50;

L2 = L(7,:) + L(8,:) + L(9,:) + L(10,:);
L2 = L2 ./ 4;
L2 = L2 - avg;

maxEhLength = 0;
temp = 0;
for i=0:length(L2-len) 
    
end

disp(L(:,6));
